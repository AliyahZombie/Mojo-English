# Mojo 仓库复盘报告

## 总体结论

本仓库是一个可用的原型，整体完成度已经达到可演示、可试用的 MVP 水平，综合评级约为 **C+**。项目的主链路是完整的，功能方向清晰，技术选型也符合纯前端产品的实现方式。

需要特别说明的是，**这是一个纯前端应用，API Key / Token 存储在浏览器侧是已确认的项目约束**，不应把“本地保存密钥”本身视为阻塞性问题。当前更需要关注的，是浏览器直连第三方服务时暴露出来的风险边界，以及浏览器运行时天然可见、可调试、可篡改的能力范围。

## 纯前端边界与风险说明

### 可接受的部分

- API Key、QStash Token、Webhook 配置保存在浏览器侧，符合本项目的纯前端定位。
- 依赖前端直连第三方接口完成 AI 调用与通知测试，是当前架构下的合理实现。
- IndexedDB、Zustand 持久化、前端配置页集中管理，都属于这个产品形态下可以接受的实现方式。

### 仍然需要处理的部分

- 浏览器运行时中的密钥和请求参数始终可被开发者工具、注入脚本、恶意扩展或 XSS 读取。
- 直连第三方 API 时，浏览器侧会暴露请求目标、请求体、响应内容和部分认证上下文。
- 前端代码里如果包含未完成的流式逻辑、调试日志或临时实现，会进一步放大运行时暴露面。
- 因此，安全表述应从“本地存储密钥是否存在”调整为“浏览器侧暴露面是否被控制住、是否存在不必要的扩散和误用”。

## 主要问题

1. **`src/services/llm.ts` 的 Gemini 流式实现未完成**
   - 这里的流式路径存在明显的未收口逻辑，注释和代码状态都说明它还停留在半成品阶段。
   - 这会直接影响 AI 聊天体验，也会让维护者误判该能力已经稳定可用。

2. **`src/pages/Setup.tsx` 体量过大**
   - 该文件同时承担配置、导入、通知测试、调度管理、偏好设置等职责，已经接近页面级“巨石组件”。
   - 结果是阅读成本高、状态耦合重、后续改动容易引入回归。

3. **第三方访问风险的表述需要重构**
   - 当前真正的风险不是“密钥放在前端”，而是“浏览器直接访问第三方服务”带来的可见性和可篡改性。
   - 这个边界需要在文档和代码注释里说清楚，否则容易把合理架构误判成安全缺陷。

4. **store 封装偏弱，状态边界不够清晰**
   - `useAppStore` 同时承载配置、Provider、通知、Deck、Essay 等多类状态，职责过宽。
   - 这种集中式设计在早期能跑，但会持续放大耦合和测试难度。

## 可维护性问题

- `src/pages/Setup.tsx` 过长，内部逻辑重复较多，后续很难通过局部修改保持一致性。
- `src/store/useAppStore.ts` 聚合过多领域状态，领域边界不清晰。
- `src/services/llm.ts` 同时处理多供应商协议、同步请求和流式解析，建议拆分协议适配层。
- 调试式日志和临时注释偏多，说明实现过程里有较多试错痕迹，后续应清理。

## 中优先级问题

- 通知、Webhook、QStash 相关流程可用，但页面内逻辑较密集，应该尽快拆出服务层或子组件。
- 设置页里混杂了多个独立能力模块，虽然不影响当前可用性，但会拖慢迭代速度。
- 部分状态与配置的同步方式偏直接，未来若增加更多 Provider 或通知方式，扩展成本会明显上升。

## 工程与过程发现

- 项目结构整体清晰，入口、页面、store、service 的分层基本成立。
- 这是一个明显以浏览器为边界的产品，不依赖后端核心业务链路，方向和实现是一致的。
- 当前代码更像“可运行的整合型原型”，而不是高度模块化的成熟应用。
- 维护节奏上看，功能推进快于结构收敛，适合先补最关键的工程收口，再继续扩功能。

## 优点

- 主流程完整，能支持配置、导入、复习、聊天、词典和通知等核心体验。
- 技术栈现代，前端能力覆盖面足，适合快速迭代。
- 本地优先的设计符合产品定位，也降低了部署复杂度。
- 主要数据和配置都已在浏览器内形成闭环，演示和试用门槛低。

## 第一批修复清单

1. 收口 `src/services/llm.ts` 中 Gemini 的流式实现，补齐完整可用路径。
2. 拆分 `src/pages/Setup.tsx`，把通知、Provider、导入、偏好设置分成更小的模块。
3. 收紧 `src/store/useAppStore.ts` 的职责边界，减少跨领域状态耦合。
4. 清理并统一关于第三方直连的风险说明，把“浏览器暴露面”作为核心表述。
5. 去除调试日志和临时注释，降低原型痕迹。

## 结论

这是一个已经能跑起来、也能展示价值的纯前端产品，但还处在结构收敛阶段。把“前端密钥存储”明确为项目边界之后，真正需要优先处理的就是浏览器直连第三方服务的暴露面控制、流式实现收口，以及配置页和状态层的拆分。
